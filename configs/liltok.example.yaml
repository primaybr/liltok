# liltok Gateway Configuration
server:
  host: "127.0.0.1"
  port: 8080
  read_timeout_seconds: 60
  write_timeout_seconds: 300

storage:
  db_path: "~/.liltok/liltok.db"
  wal_mode: true

cache:
  l1_max_entries: 10000
  default_ttl_seconds: 604800 # 7 days
  cache_nonzero_temperature: false
  semantic_cache_enabled: false
  semantic_threshold: 0.95
  prune_diffs: true

log:
  level: "info"
  format: "console"

providers:
  openai:
    api_key: ""
    base_url: "https://api.openai.com/v1"
  anthropic:
    api_key: ""
    base_url: "https://api.anthropic.com"
  nvidianim:
    api_key: ""
    base_url: "https://integrate.api.nvidia.com/v1"
  groq:
    api_key: ""
    base_url: "https://api.groq.com/openai/v1"
  gemini:
    api_key: ""
    base_url: "https://generativelanguage.googleapis.com"
  openrouter:
    api_key: ""
    base_url: "https://openrouter.ai/api/v1"
  kilo:
    api_key: ""
    base_url: "https://api.kilo.ai/api/gateway"
  cline:
    api_key: ""
    base_url: "https://api.cline.bot/api/v1"
  ollama:
    base_url: "http://localhost:11434"

routes:
  default_strategy: "auto-resilient" # "auto-resilient" | "free-first" | "premium-only"
  attempt_timeout_seconds: 45 # per non-premium upstream attempt before failing over; 0 disables
  # capture_dir: ./captures # debug only: writes a replay fixture (full conversation) per routed /v1/messages request
