{
  "description": "OpenAI ingress: a provider returns a structured tool call. The client must receive it as a tool_call with finish_reason tool_calls, whole and streamed.",
  "origin": "M7.3 OpenAI ingress replay",
  "ingress": "openai",
  "known_bug": "the json run passes; the sse run fails because the OpenAI SSE replay (cache.replayOpenAISSE) emits only the message content and a hardcoded finish_reason stop, dropping tool_calls",
  "request": {
    "model": "gpt-4o",
    "max_tokens": 1024,
    "tools": [
      {
        "type": "function",
        "function": {
          "name": "Glob",
          "description": "Find files by pattern",
          "parameters": {
            "type": "object",
            "properties": {
              "pattern": {
                "type": "string"
              }
            },
            "required": [
              "pattern"
            ]
          }
        }
      }
    ],
    "messages": [
      {
        "role": "user",
        "content": "List the Go files."
      }
    ]
  },
  "attempts": [
    {
      "tool_calls": [
        {
          "name": "Glob",
          "arguments": {
            "pattern": "**/*.go"
          }
        }
      ]
    }
  ],
  "expect": {
    "attempts": 1,
    "stop_reason": "tool_calls",
    "blocks": [
      {
        "type": "tool_use",
        "name": "Glob",
        "input": {
          "pattern": "**/*.go"
        }
      }
    ]
  }
}
